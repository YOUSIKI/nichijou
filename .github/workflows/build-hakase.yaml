name: Build hakase
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "*"
  workflow_dispatch:
jobs:
  call-workflow-passing-data:
    uses: ./.github/workflows/build-nixos.yaml
    with:
      hostname: hakase
    secrets: inherit
