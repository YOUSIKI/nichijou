name: Build satoshi
on:
  push:
    branches:
      - main
      - update_flake_lock_action
  pull_request:
    branches:
      - update_flake_lock_action
  workflow_dispatch:
jobs:
  call-workflow-passing-data:
    uses: ./.github/workflows/build-macos.yaml
    with:
      hostname: satoshi
    secrets: inherit
